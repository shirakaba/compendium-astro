---
title: Xcode
tableOfContents: false
sidebar:
  label: Xcode build phases
---

import { XcodeWindow } from '../../../components/xcode/window';
import { phasesIosApp, phasesExpoApp } from './phases';

Xcode builds run sequentially, in **phases**. When creating an Xcode project, depending on the type (e.g. app, static library, framework), you'll find different build phases get set up for you in advance.

<div class="px-[16px]">
  <XcodeWindow
    client:only
    className="not-content [&>div]:max-w-[942px]"
    phasesSetter={[phasesIosApp]}
  />
</div>

When creating a project using a framework such as React Native, you may notice additional phases beyond the ones those built-in templates provide. These are the secret sauce behind how the framework builds and runs!

<div class="px-[16px]">
  <XcodeWindow
    client:only
    className="not-content [&>div]:max-w-[942px]"
    phasesSetter={[phasesExpoApp]}
  />
</div>
